"use client";
import { useMemo, useState } from "react";
import type { News,NewsPage } from "@/lib/data";
import { NewsCard } from "./ui";
const norm=(v:string)=>v.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();
export function NewsFilter({initialPage}:{initialPage:NewsPage}){const [news,setNews]=useState<News[]>(initialPage.items);const [cursor,setCursor]=useState(initialPage.nextCursor);const [hasMore,setHasMore]=useState(initialPage.hasMore);const [loading,setLoading]=useState(false);const [active,setActive]=useState("Todas");const categories=useMemo(()=>["Todas",...Array.from(new Set(news.map(n=>n.category).filter(Boolean))).sort((a,b)=>a.localeCompare(b,"pt-BR"))],[news]);const visible=useMemo(()=>active==="Todas"?news:news.filter(n=>norm(n.category)===norm(active)),[active,news]);async function loadMore(){if(!cursor||loading)return;setLoading(true);try{const r=await fetch(`/api/feed?cursor=${encodeURIComponent(cursor)}`);if(!r.ok)throw new Error();const page=await r.json() as NewsPage;setNews(current=>[...current,...page.items.filter(item=>!current.some(existing=>existing.id===item.id))]);setCursor(page.nextCursor);setHasMore(page.hasMore)}finally{setLoading(false)}}return <div className="filter-panel"><div className="filters" aria-label="Filtrar notícias por categoria">{categories.map(o=>{const count=o==="Todas"?news.length:news.filter(n=>norm(n.category)===norm(o)).length;return <button type="button" className={`filter ${active===o?"active":""}`} onClick={()=>setActive(o)} aria-pressed={active===o} key={o}>{o} <span>{count}</span></button>})}</div><div className="results-count">{visible.length} {visible.length===1?"notícia":"notícias"}</div><div className="list">{visible.map(n=><NewsCard item={n} key={n.id}/>)}{!visible.length&&<div className="empty-state"><h3>Nenhuma notícia nesta categoria.</h3><p>Tente selecionar outro assunto.</p></div>}</div>{hasMore&&<button className="load-more" type="button" onClick={loadMore} disabled={loading}>{loading?"Carregando…":"Carregar mais notícias"}</button>}</div>}
