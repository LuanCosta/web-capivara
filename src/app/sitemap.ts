import type { MetadataRoute } from "next"; import { fallbackNews,fallbackPoliticians } from "@/lib/data";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://capivara.app.br";const routes=["","/noticias","/politicos","/em-alta","/sobre","/politica-editorial","/uso-de-ia","/politica-de-privacidade","/contato"];return [...routes.map(r=>({url:`${base}${r}`,lastModified:new Date()})),...fallbackNews.map(n=>({url:`${base}/noticias/${n.id}`,lastModified:new Date()})),...fallbackPoliticians.map(p=>({url:`${base}/politicos/${p.id}`,lastModified:new Date()}))]}
