import { AppButton,PageShell,PoliticianCard } from "@/components/ui"; import { getTrending } from "@/lib/data";
export const metadata={title:"Em alta",description:"Os políticos com maior repercussão no noticiário nas últimas 24 horas."};
export default async function EmAlta(){const ps=await getTrending();return <PageShell><section className="wrap page-hero"><span className="kicker">ÚLTIMAS 24 HORAS</span><h1>Em alta</h1><p>Os assuntos que movimentam o debate político agora, medidos pelas menções no Google News.</p></section><section className="wrap content-grid"><div className="ranking-page">{ps.slice(0,4).map((p,i)=><PoliticianCard p={p} rank={i+1} key={p.id}/>)}</div><aside className="side-card"><span className="kicker">COMO FUNCIONA</span><h3>Repercussão, não popularidade.</h3><p>O ranking mostra volume de menções no noticiário. Estar em alta não representa aprovação.</p><AppButton/></aside></section></PageShell>}
