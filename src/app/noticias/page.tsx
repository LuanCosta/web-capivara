import { getNews } from "@/lib/data"; import { AppButton,PageShell } from "@/components/ui"; import { NewsFilter } from "@/components/news-filter";
export const metadata={title:"Notícias",description:"Notícias políticas recentes, organizadas e explicadas pelo Capivara."};
export default async function Noticias(){const news=await getNews();return <PageShell><section className="wrap page-hero"><span className="kicker">NOTÍCIAS</span><h1>Entenda antes de opinar.</h1><p>Os fatos mais importantes da política brasileira, com linguagem simples e contexto.</p></section><div className="wrap content-grid"><NewsFilter news={news}/><aside className="side-card"><span className="kicker">NO APLICATIVO</span><h3>A notícia não termina na manchete.</h3><p>Veja contexto, impactos, linha do tempo e matérias relacionadas.</p><AppButton/></aside></div></PageShell>}
