import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { AppButton,PageShell } from "@/components/ui"; import { getNewsById } from "@/lib/data";
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const {id}=await params;const n=await getNewsById(id);return {title:n.title,description:n.summary,openGraph:{title:n.title,description:n.summary,images:n.image?[n.image]:[]}}}
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;if(!id)notFound();const n=await getNewsById(id);const paragraphs=n.content?.split(/\r?\n+/).map(p=>p.trim()).filter(Boolean)??[];return <PageShell><article><div className="wrap detail-hero"><div className="detail-meta"><span className="tag">{n.category}</span><span>{n.source} · {n.date}</span></div><h1>{n.title}</h1><p className="detail-subtitle">{n.summary}</p>{n.image&&<div className="detail-image" style={{backgroundImage:`url(${JSON.stringify(n.image)})`}}/>}</div><div className="wrap article">{paragraphs.length>0&&<section className="article-content"><h2>A notícia</h2>{paragraphs.map((paragraph,index)=><p key={index}>{paragraph}</p>)}{n.sourceUrl&&<a className="source-link" href={n.sourceUrl} target="_blank" rel="noopener noreferrer">Ler publicação original em {n.source}</a>}</section>}<section className="capivara-explains"><div className="explain-heading"><small>CAPIVARA EXPLICA ✨</small><h2>Os principais pontos, sem complicação</h2>{n.summaryAI&&<p>{n.summaryAI}</p>}</div>{n.explain?.length?<div className="explain-list">{n.explain.map((item,index)=><div className="explain-item" key={`${item.position}-${item.title}`}><span>{index+1}</span><div><h3>{item.title}</h3><p>{item.description}</p></div></div>)}</div>:<div className="explain"><h3>O que aconteceu</h3><p>{n.summaryAI||n.summary}</p></div>}</section><div className="cta"><h2>Acompanhe os próximos capítulos no app</h2><p>Veja contexto aprofundado, linha do tempo, repercussão e todas as matérias relacionadas — sem cadastro.</p><AppButton/></div></div></article></PageShell>}
