import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { AppButton,PageShell } from "@/components/ui"; import { getNewsById } from "@/lib/data";
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const {id}=await params;const n=await getNewsById(id);return {title:n.title,description:n.summary,openGraph:{title:n.title,description:n.summary,images:n.image?[n.image]:[]}}}
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;if(!id)notFound();const n=await getNewsById(id);return <PageShell><article><div className="wrap detail-hero"><div className="detail-meta"><span className="tag">{n.category}</span><span>{n.source} · {n.date}</span></div><h1>{n.title}</h1>{n.image&&<div className="detail-image" style={{backgroundImage:`url(${n.image})`}}/>}</div><div className="wrap article"><p className="summary">{n.summary}</p><div className="explain"><small>CAPIVARA EXPLICA ✨</small><h3>1. O que aconteceu</h3><p>{n.summary} A decisão e seus desdobramentos são acompanhados pelo Capivara a partir de fontes públicas e veículos de imprensa.</p></div><h2>O que você precisa saber</h2><p>Esta é uma prévia pública do conteúdo editorial. A apuração original é atribuída à fonte indicada, e o Capivara organiza os principais pontos para facilitar a compreensão.</p><div className="cta"><h2>Continue entendendo no app Capivara</h2><p>Veja o contexto completo, impactos, linha do tempo e todas as matérias relacionadas.</p><AppButton/></div></div></article></PageShell>}
